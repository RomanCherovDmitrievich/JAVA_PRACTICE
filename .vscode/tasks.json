{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Java",
            "type": "shell",
            "command": "mkdir -p bin && javac -d bin src/${file}.java", //Менять для определения исполняемого файла
            "group": "build",
            "problemMatcher": []
        }
    ]
}